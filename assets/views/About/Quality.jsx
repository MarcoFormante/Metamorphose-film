const Quality = ({title,text})=>{

    return (
        <div className={`quality__box quality__box--orange`}>
                <h2>{title}</h2>
                <p>{text}</p>
        </div>
    )
}

export default Quality